import React from 'react';
import ScreenContent from '../shared/ScreenContent';

export default function ExcluirScreen() {
  const handleExcluir = () => {
    // lógica de inserção
  };

  return <ScreenContent title="Excluir Usuário" onPress={handleExcluir} />;
}
